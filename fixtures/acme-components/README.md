# ACME Components synthetic fixture

Every entity, identifier, contact number, bank account, document, and amount in this directory is fictional and exists only for the VeriRemit hackathon demo.

`fixture-extraction.json` is an internal deterministic fixture. It is **not** a captured Nutrient response and must never be presented as evidence of a live sponsor integration. Live provider verification is recorded separately without secrets.
